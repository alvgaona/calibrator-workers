{
    "name": "@calibrator/unpack",
    "version": "0.0.1",
    "private": true,
    "scripts": {
        "build": "cargo build --release",
        "deploy": "wrangler deploy",
        "dev": "wrangler dev",
        "start": "wrangler dev",
        "test": "cargo test",
        "cf-typegen": "wrangler types",
        "lint": "cargo clippy"
    }
}
