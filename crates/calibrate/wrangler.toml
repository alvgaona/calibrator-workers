name = "calibrator-calibrate"
main = "build/worker/shim.mjs"
compatibility_date = "2024-09-10"

[build]
command = "cargo install -q worker-build && worker-build --release"

[[r2_buckets]]
binding = "CALIBRATOR_INPUT_BUCKET"
bucket_name = "calibrator-input"
